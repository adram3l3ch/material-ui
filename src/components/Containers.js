import { Container } from "@material-ui/core";
import React from "react";

const Containers = () => {
	return (
		<Container maxWidth="sm" style={{ background: "red" }}>
			458
		</Container>
	);
};

export default Containers;

// Attributes

//  1.maxWidth = sm, md, lg, xs
